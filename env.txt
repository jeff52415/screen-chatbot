# Gemini settings
GEMINI_API_KEY=
MODEL=gemini-2.0-flash-exp
RESPONSE_MODALITIES=TEXT

# Query settings
DEFAULT_QUERY=""
SYSTEM_PROMPT="You are a helpful assistant and answer in a friendly tone."

# Video settings
DEFAULT_VIDEO_MODE=screen # none, screen, camera
DEFAULT_MONITOR=1 # 0 is primary monitor, 1 is secondary monitor, etc.

# Audio settings
DEFAULT_AUDIO_ENABLED=true
AUDIO_DEVICE_INDEX=0
DEFAULT_AUDIO_CACHE_SECONDS=10

# Debug settings
DEFAULT_DEBUG=false